# csp-scheduling-system
Automated appointment scheduling system using constraint-based optimisation. The booking problem is modelled as a CSP and solved with Google OR-Tools (CP-SAT), enforcing hard constraints while optimising user preferences. Integrated into a web app for dynamic booking, cancellation, and rescheduling.
